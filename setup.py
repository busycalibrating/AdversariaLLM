from setuptools import setup, find_packages

setup(
    name="llm-quick-check",  # Replace with your package name
    version="0.0.1",  # Starting version
    packages=find_packages(),  # Automatically find packages in the directory
)
